%legend Lista Ordenes

= label_tag(:fecha, "Fecha:")
%input{"data-behaviour" => "datepicker", :type => "text", :data_url => "#{order_lists_path}", :value => Date.today}/

:javascript
  $(function() {
  $('[data-behaviour~=datepicker]').datepicker({
    format: 'dd-mm-yyyy'
  })
  $('[data-behaviour~=datepicker]').val("#{@date}")
  

  });

%table.table.table-condensed
  %tr
    %th Patient
    %th Acompañante
    %th
    %th
    %th

  - @order_lists.each do |order_list|
    %tr
      %td= order_list.patient.nombre
      %td
        =  if order_list.esPaciente
          x
      %td
        = link_to(order_list, "data-toggle" => "tooltip", :title => "Ver") do
          %i.icon-hand-up
      %td
        = link_to(edit_order_list_path(order_list), "data-toggle" => "tooltip", :title => "Editar") do
          %i.icon-pencil
      %td
        = link_to(order_list, "data-toggle" => "tooltip", :title => "Eliminar", :method => :delete, :data => { :confirm => 'Are you sure?' }) do
          %i.icon-trash

%br

= link_to 'Nueva Orden', new_order_list_path
