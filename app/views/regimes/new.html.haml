%h1 Nuevo Régimen

= simple_form_for(@regime) do |f|
  = f.error_notification

  .form-inputs
    %legend Nombre (10 caracteres)
    = f.input :nombre, label: false
    %legend Descripcion
    = f.input :description, label: false

  .form-actions
    = f.button :submit, 'Crear Régimen'

%a.btn{ :href => "#{cuapanel_path}" }
  %i.icon-arrow-left
  Regresar
