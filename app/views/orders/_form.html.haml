= simple_form_for(@order) do |f|
  = f.error_notification

  .form-inputs
    = hidden_field_tag 'dia[dia]', '1'
    = hidden_field_tag 'order[horario]', '1'
    = select("regime", "", Regime.all.collect {|p| [ p.nombre, p.id ] }, {:prompt => 'Seleccione Régimen'})
    #plates

    = f.input :comentarios

  .form-actions
    = f.button :submit
